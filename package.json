{
  "name": "rankfixer-site",
  "version": "1.0.0",
  "description": "RankFixer — Premium managed Filipino virtual assistant staffing",
  "scripts": {
    "build": "npx @tailwindcss/cli -i ./src/css/input.css -o ./docs/style.css --minify",
    "watch": "npx @tailwindcss/cli -i ./src/css/input.css -o ./docs/style.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^4.3.0"
  }
}